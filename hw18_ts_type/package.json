{
  "name": "hw18_ts_types",
  "version": "1.0.0",
  "description": "Домашняя работа 18",
  "license": "ISC",
  "author": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:up": "docker-compose up",
    "migration:generate": "npx sequelize-cli migration:generate --name my-migration",
    "migration:up": "npx sequelize-cli db:migrate",
    "migration:down": "npx sequelize-cli db:migrate:undo:all",
    "seed:up": "npx sequelize-cli db:seed:all",
    "seed:down": "npx sequelize-cli db:seed:undo:all",
    "start": "ts-node ./src/index.ts",
    "dev": "nodemon --exec ts-node src/index.ts",
    "build": "npx tsc"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {}
}
